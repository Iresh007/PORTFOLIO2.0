/**
 * Check if the HN Job Board feature is enabled.
 *
 * Disabled entirely — a job board isn't relevant content for this
 * portfolio site. Left as a function (rather than deleted outright) so the
 * feature can be restored later by reverting this single change.
 */
export function isJobBoardEnabled(): boolean {
  return false
}
